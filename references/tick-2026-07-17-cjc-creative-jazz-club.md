# 149th v2 tick — CJC Creative Jazz Club Aotearoa (cjc@creativejazzclub.co.nz)

- source_id: `ff512d32-db7a-4e66-9629-891900f55036` v1
- new_id: `fa99644f-9f36-473e-a218-505da6a73f2c` v2 (gap=1, clean baseline)
- envelope: Drafts 281 (APPENDUID 4 281) — NOTE: envelope 280 was deleted (see v2.7.108 below)
- eml_md5: `51ea26e25fd8b4ccee16476dbb5ef4b8` (26,887-byte `.eml`)
- to: cjc@creativejazzclub.co.nz
- subject: "Quick question for the team"
- contact: CJC Creative Jazz Club Aotearoa — Auckland creative jazz club, T1 inbox `cjc@creativejazzclub.co.nz` on 1st-party domain `creativejazzclub.co.nz`
- defects_fixed: [logo, h1, sig_pad, logo_pad]
  - logo.png × 1 → base64 inline (cached `/tmp/wa-logo.b64`)
  - H1 leak SUFFIX-LESS variant stripped via fix-script `H1_BLOCK` regex (NOT v2.7.5 EXEMPTION because leak comment `<!-- Subject-style H1 title -->` was present — disqualifying condition #1 per v2.7.100 TWO-condition gate)
  - sig padding lh:1.5 `8 32 24 32` → `8 32 8 32`
  - logo padding `16 32 24 32 align="left"` → `0 32 24 32 align="left"`
- verify_envelope.py: EXIT 0 cleanly on first try (`h1_comment_markers=0, h1_open_tags=0, hardcoded_png_imgs=0, base64_imgs=1, sig_padding_old=0, logo_padding_old=0`) — no `check_h1_exemption.py` invocation needed (the leak comment presence already disqualified EXEMPTION at the source-discriminator step)
- cross-tick md5 dedup: 0 hits (eml_md5=`51ea26e25fd8b4ccee16476dbb5ef4b8` distinct from all prior docs scanned via dual-glob per v2.7.93)
- subject template: "Quick question for the team" — third templated-name pattern (147th Christine Brotherton / 148th City studio team / 149th CJC Creative Jazz Club Aotearoa) within a 4-tick run where the subject template names a generic recipient ("the team") and the actual inbox is the canonical T1 address. Lesson reaffirmed: always extract the recipient email from the unsubscribe link, never trust the subject template.
- source status-projection mismatch recurred (~**108th confirmation**: filter=`scheduled` → initial GET=`scheduled` → final GET=`draft`; `updated_at=2026-06-23T22:38:48.226428Z` byte-identical throughout; v1 unchanged; 1m36s `created_at`-to-`updated_at` gap is a pre-existing one-shot touch from 2026-06-23, NOT this cron)
- v2.7.43 dual-filter pagination (1046 scheduled + 200 draft = 1246 candidate rows merged, 1080 → 1079 after exclusion), v2.7.45 stale-lock cleanup (0 stale), v2.7.51 self-cleanup of own `.inprogress` lock, v2.7.73 no-pipe PATCH verification (`python3 -c "import json; …"` workaround used), v2.7.74 raw-IMAP `BODY.PEEK[]` verification (via `scripts/fetch_envelope.py` 281 → 26887-byte `.eml`), v2.7.87 IMAP-UID fetch via `imap.uid('FETCH', '281', ...)`, v2.7.88 verify_envelope.py invoked via `python3 <path>`, v2.7.89 byte-identical `updated_at` discipline, v2.7.93 dual-glob md5 dedup (0 hits), v2.7.96 codification (no drift on this tick — fix-script regex counts matched raw counts 1:1), v2.7.100 TWO-condition gate re-validated (9th consecutive tick: 141 + 142 + 143 + 144 + 145 + 146 + 147 + 148 + **149** — source carried BOTH leak comment AND inline-styled `<h1>` → EXEMPTION did NOT apply), v2.7.80 source-version edge case (re-read GET showed `version=1` — POST auto-assigned `version=2`), v2.7.107 fetch_envelope.py codification used as canonical verification path. 174 step logs total. 1,079 unprocessed candidates remain after this tick (1,080 → 1,079, −1).

**NEW PITFALL DISCOVERED — v2.7.108 (URL-encoded `To:` header extraction).**

The Phase-5 `append_imap_template.py`'s email extraction regex `unsubscribe-email/\?email=([^\x27\x22<>\s]+)` is INSUFFICIENT when the source's `content` field contains a URL-encoded `%40` in the unsubscribe href (e.g. `href=".../unsubscribe-email/?email=cjc%40creativejazzclub.co.nz"`). The regex captures the email AS URL-ENCODED (`cjc%40creativejazzclub.co.nz`) and assigns verbatim to the `MIMEMultipart` `To:` header. Result: the Drafts envelope ends up with `To: cjc%40creativejazzclub.co.nz` instead of the expected `To: cjc@creativejazzclub.co.nz`.

**This was the FIRST occurrence in the v2 run.** Recovery: (a) deleted bad envelope 280 via `imap.uid("STORE", "280", "+FLAGS", "\\Deleted")` then `imap.expunge()`; (b) patched `/tmp/append_imap_ff512d32.py` to add `import urllib.parse` + `to_email = urllib.parse.unquote(to_email)` after the regex match and before `MIMEMultipart` construction; (c) re-PUSHED to envelope 281; (d) verified via raw-IMAP `BODY.PEEK[HEADER.FIELDS (FROM TO SUBJECT)]` that envelope 281 has the decoded `To: cjc@creativejazzclub.co.nz`. 26887-byte `.eml` recovered cleanly by `verify_envelope.py` (EXIT 0).

**The canonical template `templates/append_imap_template.py` is now stale.** Future ticks MUST run the patched variant (with `urllib.parse.unquote`) — codifying this as v2.7.108. Recommended fix: edit the canonical template to add the URL-decode line. This is a write-once-additive change to the template (no other behavior changes).

**Verification of fix:** after re-PUSH the second time, raw-IMAP `BODY.PEEK[HEADER.FIELDS (FROM TO SUBJECT)]` on envelope 281 returned `From: technical.support@wawadata.com\r\nTo: cjc@creativejazzclub.co.nz\r\nSubject: Quick question for the team\r\nMessage-ID: <…@wawadata.com>\r\n\r\n` — confirmed the URL-decoded `To:` form.

**Lesson for future ticks:** when extracting email from a hyperlink href, **always URL-decode the captured group** before using it as a `To:` header value. The Phase-5 recipient-extraction regex has always implicitly assumed the email was already decoded by the source fabric; this tick's source broke that assumption (the CJC Creative Jazz Club Aotearoa website encoded its unsubscribe link with `%40` rather than the literal `@`). The `urllib.parse.unquote` step is now load-bearing — promote to v2.7.108 and add it to the canonical template.

**Three non-pitfall observations worth recording:**

1. **v2.7.100 TWO-condition gate re-validated for a 9th consecutive tick (141 + 142 + 143 + 144 + 145 + 146 + 147 + 148 + 149).** This tick's source carried BOTH `<!-- Subject-style H1 title -->` (suffix-less leak form, disqualifying condition #1) AND an `<h1 style="margin:0; font-size:22px; line-height:1.3; color:#1f2937; font-weight:600;">` open tag with all 4 canonical style fields in range (satisfied condition #2). Result: **EXEMPTION did NOT apply** — the leak comment's presence overrides the inline-styled `<h1>`. The fix script's `H1_BLOCK` regex (v2.7.18 permissive pattern with `re.DOTALL`) correctly stripped the entire block (comment + `<h1>` + `</tr>`) and `verify_envelope.py` exited 0 cleanly on first try without needing `check_h1_exemption.py`. **Lesson reaffirmed at the 149th tick**: the TWO-condition gate is now codified across 9 consecutive ticks — when source has BOTH leak comment AND inline-styled `<h1>`, the fix script handles it cleanly; no EXEMPTION gate needed. Reference: `references/v2.7.5-exemption-dual-verifier-pattern.md` "TWO required conditions" section.

2. **Templated-name-vs-recipient-inbox pattern re-validated (3rd tick).** The 149th-tick source subject was `Quick question for the team` but the actual recipient inbox is `cjc@creativejazzclub.co.nz` at CJC Creative Jazz Club Aotearoa — the templated "the team" string is a generic marketing-segment label, NOT the inbox name. This is the **3rd confirmation** of the pattern (144th Chris / Yeoman Survey / subject "Quick question for Mathew Mortell" routing to `chris@yeomansurvey.co.nz`; 148th City studio team / `city@funis-training.com`; **149th CJC Creative Jazz Club / `cjc@creativejazzclub.co.nz`**). **Lesson**: when checking the recipient inbox, **always extract the email from the unsubscribe link in `content`** (the canonical Phase-5 recipient-extraction mechanism in `append_imap_template.py`); do NOT trust the subject template for inbox identification.

3. **v2.7.107 fetch_envelope.py codified as canonical verification path (1st tick using it).** This tick successfully invoked `python3 /home/hex-bot/.hermes/skills/wawa-edm-push-drafts-to-himalaya/scripts/fetch_envelope.py 281 /tmp/wawa-edm-push-batch/envelope_281.eml` after re-PUSH (post-fix). The script's runtime shape-probe (v2.7.92) handled the FETCH response cleanly — wrote 26887 bytes, `verify_envelope.py` consumed it without any recovery path being triggered. Confirms v2.7.107 codification works in production. The hand-written raw IMAP probe recipes documented in `references/v2.7.74-imap-body-peek-verification-recipe.md` are demoted to fallback status.

H1 SUFFIX-LESS form running tally bumped to 20/25 since 92nd tick (~80%); SUFFIXED 5/25 (~20%) — stable around the v2.7.95/v2.7.96/v2.7.99/v2.7.100/v2.7.102/v2.7.103/v2.7.104/v2.7.105/v2.7.106/v2.7.107 heuristic. Source status-projection mismatch bumped to ~108th confirmation (canonical variant): filter=`scheduled` → initial GET=`scheduled` → final GET=`draft`; `updated_at` byte-identical; v1 unchanged.

Reference: `references/tick-2026-07-17-cjc-creative-jazz-club.md`.
